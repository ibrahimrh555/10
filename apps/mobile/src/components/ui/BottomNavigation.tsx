import { Pressable, StyleSheet, View } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { theme } from "@/design-system";
import { AppText } from "./AppText";
const items = [{ label: "Accueil", icon: "home-outline" }, { label: "Explorer", icon: "football-outline" }, { label: "Créer", icon: "add-circle-outline" }, { label: "Profil", icon: "person-outline" }] as const;
export function BottomNavigation() { return <View style={styles.bar}>{items.map((item, index) => <Pressable accessibilityRole="tab" accessibilityState={{ selected: index === 0 }} key={item.label} style={styles.item}><Ionicons name={item.icon} size={24} color={index === 0 ? theme.colors.primary : theme.colors.textInverse} /><AppText variant="caption" color={index === 0 ? "primary" : "textInverse"}>{item.label}</AppText>{index === 0 ? <View style={styles.active} /> : null}</Pressable>)}</View>; }
const styles = StyleSheet.create({ bar: { backgroundColor: theme.colors.surfaceBrand, borderRadius: theme.radius.xl, flexDirection: "row", minHeight: theme.sizes.control.bottomNavigation, overflow: "hidden", paddingHorizontal: theme.spacing.xxs, paddingTop: theme.spacing.xs, ...theme.shadows.md }, item: { alignItems: "center", flex: 1, gap: 3, justifyContent: "center", minHeight: 56, position: "relative" }, active: { backgroundColor: theme.colors.primary, borderRadius: theme.radius.full, bottom: 0, height: 3, position: "absolute", width: 38 } });
